#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - main block
 * Return: Always 0 (Success)
 */

int main(void)
{
	char low, c, r;

	c = 'c';
	r = 'r'
	
	for (low ='a'; low <= 'z'; low++)
	{
		if (low != e && low != r)
		{
			putchar(low);
		}

	}

	putchar('\n');
	return (0);
}
